package org.kirhgoff.phoneprettifier.mechanics;

import org.kirhgoff.phoneprettifier.model.Intermediate;
import org.kirhgoff.phoneprettifier.model.MultiWord;
import org.kirhgoff.phoneprettifier.model.NumberArray;

import java.util.Collection;
import java.util.List;

public class WordMatcher {
  public List<Intermediate> match(NumberArray array, Dictionary dictionary) {
    Collection<MultiWord> words = dictionary.getWords();

    return null;
  }
}
